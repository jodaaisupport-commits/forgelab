import {icon} from './icons.js'; import {t} from './locales/de.json';
export function layout(user,state,content){return `<div class="app-shell"><div class="mobile-backdrop" data-action="close-menu"></div><aside class="sidebar"><div class="brand"><span class="brand-mark">F</span><div><strong>ForgeLab</strong><small>AI Model Studio</small></div><button class="icon-button mobile-close" data-action="close-menu" aria-label="Menü schließen">×</button></div><div class="workspace"><small>WORKSPACE</small><strong>${state.settings.workspace}</strong><span class="plan">Starter Plan · Demo</span><div class="usage"><span>Nutzung</span><b>24 %</b><i><em></em></i></div></div><nav>${[['dashboard','grid'],['models','cube'],['datasets','database'],['training','activity'],['playground','play'],['connectors','plug'],['admin','users'],['settings','settings']].map(([key,ic])=>`<button class="nav-item ${state.view===key?'active':''}" data-view="${key}">${icon(ic)}${t(key)}</button>`).join('')}</nav><button class="export-nav" data-action="export">${icon('download')} ${t('download')}</button><div class="profile"><span class="avatar">${user.name[0]}</span><div><strong>${user.name}</strong><small>${user.role}</small></div><button class="icon-button" data-action="logout" aria-label="Abmelden">↪</button></div></aside><main class="main"><header><button class="menu-button" data-action="open-menu" aria-label="Menü öffnen">☰</button><div><small class="breadcrumb">Meine Werkstatt / ${t(state.view)}</small><h1>${pageTitle(state.view)}</h1></div><div class="header-actions"><span class="status-dot">● Demo</span><span class="avatar small">${user.name[0]}</span></div></header><section class="content">${content}</section></main></div>`}
function pageTitle(v){return ({dashboard:'Übersicht',models:'Modelle',datasets:'Deine Datensätze',training:'Trainingscenter',playground:'Teste dein Modell',connectors:'Deine Connectoren',admin:'Admin CMS Dashboard',settings:'Einstellungen'})[v]||'ForgeLab'}
export const button=(label,action,kind='primary')=>`<button class="btn ${kind}" data-action="${action}">${label}</button>`;
